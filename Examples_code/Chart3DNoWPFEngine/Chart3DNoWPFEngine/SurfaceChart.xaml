<Window x:Class="Chart3DNoWPFEngine.SurfaceChart"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Surface Chart" Height="400" Width="450">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>        
        <Grid x:Name="chartGrid" SizeChanged="chartGrid_SizeChanged" Margin="10 10 0 10" Grid.Row="0" Grid.Column="0">
            <Canvas x:Name="chartCanvas" Background="Transparent" ClipToBounds="True"/>
        </Grid>
        <Grid x:Name="colorbarGrid" Margin="0 10 10 10" Grid.Row="0" Grid.Column="1">
        </Grid>
    </Grid>
</Window>


